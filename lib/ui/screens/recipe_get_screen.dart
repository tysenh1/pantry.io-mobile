import 'package:flutter/material.dart';
import 'package:pantry_io_mobile/ui/widgets/recipe_get_widget.dart';

class RecipeGetScreen extends StatelessWidget {
  const RecipeGetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: RecipeGetWidget());
  }
}
